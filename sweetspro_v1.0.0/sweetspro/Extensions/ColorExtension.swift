import SwiftUI

extension Color {
    // Elegant Gold/Brown theme extracted from site feel
    static let sweetsGold = Color(red: 0.65, green: 0.55, blue: 0.25) // Approx gold
    static let sweetsBrown = Color(red: 0.35, green: 0.25, blue: 0.20) // Dark brown text
    static let sweetsLightBg = Color(red: 0.98, green: 0.98, blue: 0.97) // Warm off-white
    static let sweetsGray = Color(red: 0.6, green: 0.6, blue: 0.6)
}
